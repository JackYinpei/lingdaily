services:
  app:
    image: ghcr.io/jackyinpei/lingdaily:latest
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./podcasts:/app/public/podcasts
    restart: unless-stopped
